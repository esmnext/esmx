// Under the module protocol v2 there is no directory-scope remapping:
// the Vue 2 copy is addressed explicitly through the "vue2" npm alias, which
// ships vue 2.7's own types — so no type suppression is needed.
export { version } from 'vue2';
