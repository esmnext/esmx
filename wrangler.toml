name = "esmx"
compatibility_date = "2024-01-01"

[build]
command = "corepack enable && corepack prepare pnpm@10 --activate && node ./build.mjs"
cwd = "."

[build.environment_variables]
NODE_VERSION = "22.16.0"

[assets]
directory = "./dist" 